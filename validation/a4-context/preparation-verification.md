# 本机移交材料检查

日期：2026-09-17。范围：正式方案、材料组织、原有 H1 诊断器副本与诊断配置示例。

- H1 两个 JavaScript 文件与历史版本逐字节相同；新位置为 `validation/a4-context/h1/`。
- 在 Node v24.15.0 上执行 `node --test validation/a4-context/h1/capture-hook.test.mjs`：16 项通过，0 失败。
- 正式方案、移交材料和更新的路线图已核对本地链接/锚点、代码块及表格。
- 环境记录模板可解析为 JSON。
- 提取 H1 README 中的 Python 配置生成示例，在含空格的临时目录执行；生成的两类 Hook 注册有效。使用人工 stdin 事件运行生成的命令，正确保存事件并输出对应 JSON。

这些检查证明材料结构和已有诊断器可用。**本轮未启动 Claude Code、未执行模型请求、未运行语义 A/B 对照、未实现 B 的语义注入器。** 人工 stdin 往返不计为真实宿主协议通过。

迁移包仅包含显式选择的方法文档、验证材料和试点只读输入；不包含原机用户配置、凭据或完整日志。包根清单记录逐文件 SHA256；`pilot-inputs/SNAPSHOT.json` 另记项目来源、选择范围与输入哈希。解包后的诊断器测试用于检查路径迁移，不扩大宿主支持范围。
