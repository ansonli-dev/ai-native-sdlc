# AI SDLC 术语

[返回总览与下一步](README.md) · 术语速查

本词表统一本套件的三个步骤与三个主要产物：Intent、Spec、Plan。步骤以目标产物命名，分别是明确意图、形成规格、制定计划。

## Language

### 需求范围

需求可以大也可以小；本方法只要求明确业务目标、本次负责范围与验收边界。默认采用一份 Intent、一份 Spec 和一份 Plan，需要独立设计、验收或交接时再拆分，并引用共同依据。具体规则见[范围与复用](docs/method/sdd-artifact-contracts.md#scope)。

**Feature / Story（可选外部分类）**：
项目已有需求系统中的功能或业务增量标签，其层级含义沿用该项目约定。可关联到相应 Intent/Spec 范围；本套件不要求先分类或固定按 Feature → Story 拆分，也不据标签决定必须创建哪些文件。

### 三个主要产物

**Intent（变更意图）**：
由 BA 负责生成、维护并确认业务表述的需求意图记录，明确为什么改变、预期结果、范围、约束和业务决定，并引用已有需求、方案与原型，为 Spec 提供业务依据。AI 可协助起草，尚未确认部分保留状态；超出 BA 授权的业务取舍由现有决策人决定。Intent 可以覆盖不同大小的需求。
_Avoid_：把未经整理的输入直接视为完整 Intent，或把尚待讨论的方案当作已确定要求；已有充分成果应直接复用。

**Spec（变更规格）**：
依据 Intent 与现状形成的需求与方案记录，明确目标行为、验收条件、关键实现方式及选择依据，为 Plan 提供可拆解的方案。
_Avoid_：用 Spec 仅指需求，或把执行任务状态纳入其职责。

**Plan（实施计划）**：
依据 Spec 组织的实施安排，明确工作单元、文件落点、依赖、验证方法、完成条件与进度，为执行者提供下一项可完成的工作。
_Avoid_：在 Plan 中另存一份权威方案，或通过任务描述改变上游目标。

### 产物内部的内容

**Design（方案设计）**：
Spec 中面向工程交付的实现方式与选择依据，包括关键模块、交互、接口、数据、技术取舍和验证策略。BA 的业务或交互设计可以在 Intent 前或过程中形成，Spec 继承已确定内容并补齐工程方案。
_Avoid_：将 Design 作为与 Intent、Spec、Plan 并列的第四个步骤或产物。

**Task（执行任务） / Tasks（任务清单）**：
Task 是具有明确结果、范围、依赖和验证方式的工作单元；Tasks 是 Plan 中组织这些工作单元及状态的内容。
_Avoid_：将 Tasks 作为独立主线步骤，将业务需求条目等同于工程 Task，或要求每项任务对应一次提交、一个独立上线单元。一项需求可以通过若干 Task 实现，共享 Task 可以支持多项需求。

### 工程决定与记录职责

**工程负责人**：
对本次范围内关键技术决定和实施安排承担接受责任的人，可以由实施开发者兼任，并可在自身权限内明确委托决定范围。
_Avoid_：默认等同于技术总监或独立审批岗位；把委托给 Agent 等同于转移最终责任。

**接受依据**：
能够定位有权者已接受哪些决定、适用修订与范围的记录或授权来源。
_Avoid_：用文档生成、检查通过或工具允许操作代替决定的接受。

**权威记录**：
对某类产物及指定范围保存有效内容和修订依据的唯一正式记录；仓库或经项目明确指定的外部系统均可承担此职责。
_Avoid_：默认把最后修改的副本、聊天摘要或未同步的工作文件当作正式记录。

**Colla**：
当前团队使用的需求与进度管理产品，承接 BA 生成的 Intent 内容，并管理对应事项的整体进度。
_Avoid_：把事项状态等同于业务正文修订、工程 Task 状态或实际验证结果。

**变更关闭**：
本次需求的全部约定范围及项目要求的验收条件均已满足后的整体结论。
_Avoid_：用单个 Task 完成、单个 MR 获批或部分范围合并代替整体关闭；把关闭理解为删除需求正文。

### 跨变更复用的项目依据

**ADR（项目决策记录）**：
本套件用 ADR 统一记录需要跨变更复用的工程约定与设计决定，包括适用范围、决定及理由、约束和有效状态。项目规范是其中约定的内容；Spec、Plan 引用并可推动其演进。
这里的统一范围是工程规范与决定。Intent 默认依据业务来源形成；ADR 从 Spec 开始作用于方案与实施，冲突不能自动改写需求。已有记录中的业务条款按其来源和权限判断效力。
_Avoid_：将工程规范与 ADR 作为两套并行的权威记录，将 ADR 已接受等同于已经实现，或要求业务需求先符合历史技术选型；ADR 不增加主线步骤。
